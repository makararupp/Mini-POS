spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mini-pos
    username: mini-pos
    password: minipos123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  servlet:
    context-path: /api/v1